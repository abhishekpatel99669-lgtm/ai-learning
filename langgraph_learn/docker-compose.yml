services:
    mongodb:
        image: mongo
        ports:
          - "27017:27017"
        environment:
          MONGO_INITDB_ROOT_USERNAME: abhi
          MONGO_INITDB_ROOT_PASSWORD: abhi
        volumes:
          - mongodb_data:/data/db
volumes:
  mongodb_data: